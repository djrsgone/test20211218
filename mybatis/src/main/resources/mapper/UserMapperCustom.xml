<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.djr.demo.mybatis.my.mapper.UserMapperCustom">
    <select id="queryByIdCustom" parameterType="int" resultType="com.djr.demo.mybatis.pojo.User">
        SELECT id, username, password FROM tb_user WHERE id = #{sId}
    </select>
</mapper>